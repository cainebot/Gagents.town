---
phase: 01-app-shell
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/login/page.tsx
  - src/components/atoms/BrandMark.tsx
autonomous: true
requirements: [SHELL-03, SHELL-05]

must_haves:
  truths:
    - "Login page renders entirely with UUI tokens — zero legacy var(--*) inline styles"
    - "BrandMark component uses UUI font and color tokens exclusively"
    - "Login form card, input, button, and error message all use UUI semantic tokens"
  artifacts:
    - path: "src/app/login/page.tsx"
      provides: "Login page with UUI tokens"
      min_lines: 30
    - path: "src/components/atoms/BrandMark.tsx"
      provides: "Brand mark with UUI tokens"
      contains: "font-display"
  key_links:
    - from: "src/app/login/page.tsx"
      to: "UUI tokens"
      via: "Tailwind classes with var(--bg-*), var(--text-*), var(--brand-*)"
      pattern: "var\\(--bg-|var\\(--text-|var\\(--brand-"
    - from: "src/components/atoms/BrandMark.tsx"
      to: "UUI tokens"
      via: "Tailwind classes replacing inline styles"
      pattern: "font-display|var\\(--brand-"
---

<objective>
Migrate the login page and BrandMark component from legacy var(--*) tokens to UUI semantic tokens.

Purpose: Login page is a standalone surface that must use UUI tokens. BrandMark is a shared header component used in the sidebar.
Output: Login page and BrandMark with zero legacy tokens, all styling via Tailwind + UUI custom properties.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Token mapping reference for this plan -->

Legacy → UUI replacement:
- var(--background) → bg-[var(--bg-primary)]
- var(--card) → bg-[var(--bg-secondary)]
- var(--card-elevated) → bg-[var(--bg-tertiary)]
- var(--border) → border-[var(--border-primary)]
- var(--accent) → text-[var(--brand-600)] / bg-[var(--brand-600)]
- var(--text-primary) → text-[var(--text-primary-900)]
- var(--text-secondary) → text-[var(--text-secondary-700)]
- var(--text-muted) → text-[var(--text-quaternary-500)]
- var(--font-heading) → font-display (class, maps to Sora via typography.css)
- var(--font-body) → font-[family-name:var(--font-text)]
- var(--error) → text-[var(--error-600)]
- var(--error-bg) → bg-[var(--error-600)]/10
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate login page to UUI tokens</name>
  <files>src/app/login/page.tsx</files>
  <read_first>
    - src/app/login/page.tsx (current login page — extensive inline var(--*) styles)
  </read_first>
  <action>
Replace every inline style={{ ... }} with equivalent Tailwind utility classes using UUI tokens. The file must have ZERO `style={{` attributes when done. Specific replacements:

**LoginPage component (outer wrapper):**
- Remove `style={{ backgroundColor: 'var(--background)' }}` from outer div
- Add class `bg-[var(--bg-primary)]` to existing `className="min-h-screen flex items-center justify-center p-4"`

**Suspense fallback div:**
- Remove `style={{ backgroundColor: 'var(--card)', border: '1px solid var(--border)' }}`
- Add classes: `bg-[var(--bg-secondary)] border border-[var(--border-primary)]` to existing className

**LoginForm card div (className="rounded-xl p-10"):**
- Remove `style={{ backgroundColor: 'var(--card)', border: '1px solid var(--border)' }}`
- Add classes: `bg-[var(--bg-secondary)] border border-[var(--border-primary)]`

**Terminal icon:**
- Remove `style={{ color: 'var(--accent)' }}`
- Add class: `text-[var(--brand-600)]`

**h1 "Mission Control":**
- Remove `style={{ fontFamily: 'var(--font-heading)', color: 'var(--text-primary)', letterSpacing: '-0.5px' }}`
- Add classes: `font-display text-[var(--text-primary-900)] tracking-tight`

**p "Introduce la contrasena":**
- Remove `style={{ color: 'var(--text-secondary)' }}`
- Add class: `text-[var(--text-secondary-700)]`

**Lock icon:**
- Remove `style={{ color: 'var(--text-muted)' }}`
- Add class: `text-[var(--text-quaternary-500)]`

**Password input:**
- Remove `style={{ backgroundColor: 'var(--card-elevated)', border: '1px solid var(--border)', color: 'var(--text-primary)' }}`
- Add classes: `bg-[var(--bg-tertiary)] border border-[var(--border-primary)] text-[var(--text-primary-900)]`
- Add focus ring: `focus:outline-none focus:ring-1 focus:ring-[var(--brand-600)]`

**Error message div:**
- Remove `style={{ backgroundColor: 'var(--error-bg)', color: 'var(--error)' }}`
- Add classes: `bg-[var(--error-600)]/10 text-[var(--error-600)]`

**Submit button:**
- Remove `style={{ backgroundColor: 'var(--accent)', color: 'white' }}`
- Add classes: `bg-[var(--brand-600)] text-white hover:bg-[var(--brand-700)]`

**Footer p:**
- Remove `style={{ color: 'var(--text-muted)' }}`
- Add class: `text-[var(--text-quaternary-500)]`

Keep all logic, imports, state management, and form handling unchanged.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-c && grep -c "style={{" src/app/login/page.tsx | grep "^0$" && grep -c "var(--card)" src/app/login/page.tsx | grep "^0$" && grep -c "var(--accent)" src/app/login/page.tsx | grep "^0$" && grep -c "var(--background)" src/app/login/page.tsx | grep "^0$" && grep -c "var(--font-heading)" src/app/login/page.tsx | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/login/page.tsx contains ZERO `style={{` attributes
    - src/app/login/page.tsx does NOT contain `var(--card)`, `var(--accent)`, `var(--background)`, `var(--border)` (without UUI prefix), `var(--text-primary)` (legacy form), `var(--text-secondary)` (legacy form), `var(--text-muted)`, `var(--error-bg)`, `var(--error)` (legacy form), `var(--font-heading)`, `var(--card-elevated)`
    - src/app/login/page.tsx contains `bg-[var(--bg-primary)]`
    - src/app/login/page.tsx contains `bg-[var(--bg-secondary)]`
    - src/app/login/page.tsx contains `bg-[var(--bg-tertiary)]`
    - src/app/login/page.tsx contains `text-[var(--brand-600)]`
    - src/app/login/page.tsx contains `bg-[var(--brand-600)]`
    - src/app/login/page.tsx contains `font-display`
    - src/app/login/page.tsx contains `text-[var(--error-600)]`
    - src/app/login/page.tsx contains `border-[var(--border-primary)]`
  </acceptance_criteria>
  <done>Login page uses only UUI semantic tokens via Tailwind classes. Zero inline styles. Zero legacy var(--*) tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate BrandMark to UUI tokens</name>
  <files>src/components/atoms/BrandMark.tsx</files>
  <read_first>
    - src/components/atoms/BrandMark.tsx (current BrandMark with var(--accent), var(--font-heading), var(--text-primary), var(--text-secondary))
  </read_first>
  <action>
Replace all legacy tokens in BrandMark.tsx with UUI equivalents. Specific changes:

1. The logo square div: `bg-[var(--accent)]` is already close but uses legacy token. Change to `bg-[var(--brand-600)]`.
   - Current: `className="grid h-10 w-10 place-items-center rounded-lg bg-[var(--accent)] text-xs font-semibold text-white"`
   - New: `className="grid h-10 w-10 place-items-center rounded-lg bg-[var(--brand-600)] text-xs font-semibold text-white"`

2. The "DC" span inside logo: Remove `style={{ fontFamily: 'var(--font-heading)', letterSpacing: '0.2em' }}`
   - Replace with: `className="font-display tracking-[0.2em]"`

3. "THE DIGITAL" div: Remove `style={{ fontFamily: 'var(--font-heading)' }}` and update className
   - Current className: `"text-sm uppercase tracking-[0.26em] text-[var(--text-primary)]"`
   - New className: `"font-display text-sm uppercase tracking-[0.26em] text-[var(--text-primary-900)]"`
   - Remove the `style` attribute entirely

4. "Circus" div: `text-[var(--text-secondary)]` → `text-[var(--text-secondary-700)]`
   - Current: `className="text-[11px] font-medium text-[var(--text-secondary)]"`
   - New: `className="text-[11px] font-medium text-[var(--text-secondary-700)]"`

The file should have ZERO `style={{` attributes when done.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-c && grep -c "style={{" src/components/atoms/BrandMark.tsx | grep "^0$" && grep -c "var(--accent)" src/components/atoms/BrandMark.tsx | grep "^0$" && grep -c "var(--font-heading)" src/components/atoms/BrandMark.tsx | grep "^0$" && grep "font-display" src/components/atoms/BrandMark.tsx && grep "var(--brand-600)" src/components/atoms/BrandMark.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/atoms/BrandMark.tsx contains ZERO `style={{` attributes
    - src/components/atoms/BrandMark.tsx does NOT contain `var(--accent)`
    - src/components/atoms/BrandMark.tsx does NOT contain `var(--font-heading)`
    - src/components/atoms/BrandMark.tsx does NOT contain `var(--text-primary)` (without -900 suffix)
    - src/components/atoms/BrandMark.tsx does NOT contain `var(--text-secondary)` (without -700 suffix)
    - src/components/atoms/BrandMark.tsx contains `bg-[var(--brand-600)]`
    - src/components/atoms/BrandMark.tsx contains `font-display` (at least 2 occurrences — logo span and title div)
    - src/components/atoms/BrandMark.tsx contains `text-[var(--text-primary-900)]`
    - src/components/atoms/BrandMark.tsx contains `text-[var(--text-secondary-700)]`
  </acceptance_criteria>
  <done>BrandMark uses only UUI tokens. font-display for headings, brand-600 for accent, text-primary-900 and text-secondary-700 for text. Zero inline styles.</done>
</task>

</tasks>

<verification>
- `grep -r "style={{" src/app/login/page.tsx src/components/atoms/BrandMark.tsx` returns no matches
- `grep -rE "var\(--(background|card|accent|font-heading|font-body|text-primary|text-secondary|text-muted|error-bg|error|border|card-elevated|surface)\)" src/app/login/page.tsx src/components/atoms/BrandMark.tsx` returns no matches (only UUI-prefixed tokens like --bg-primary, --brand-600 should remain)
</verification>

<success_criteria>
- Login page has zero inline styles and zero legacy var(--*) tokens
- BrandMark has zero inline styles and zero legacy var(--*) tokens
- Both files use only UUI semantic tokens via Tailwind utility classes
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell/01-02-SUMMARY.md`
</output>
