---
phase: 03-wave-c-agents-+-office
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/office/page.tsx
autonomous: true
requirements: [WAVC-06]

must_haves:
  truths:
    - "Office page React chrome renders with zero var(--*) inline styles"
    - "Phaser canvas internals (PhaserGame, AgentPanel, EventBridge) are NOT modified"
    - "Hardcoded hex colors for toast system and connection indicator replaced with UUI semantic tokens"
  artifacts:
    - path: "src/app/(dashboard)/office/page.tsx"
      provides: "Office page with UUI tokens on React chrome"
      contains: "text-primary"
  key_links:
    - from: "src/app/(dashboard)/office/page.tsx"
      to: "PhaserGame"
      via: "dynamic import (allowlisted)"
      pattern: "dynamic.*PhaserGame"
---

<objective>
Migrate office page React chrome from legacy var(--*) inline styles and hardcoded hex colors to UUI Tailwind tokens. Leave Phaser canvas internals untouched.

Purpose: Eliminate legacy design system references from office page React chrome while preserving allowlisted canvas internals.
Output: One file with zero var(--*) in React chrome, Phaser imports unchanged.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate office/page.tsx React chrome to UUI tokens</name>
  <files>src/app/(dashboard)/office/page.tsx</files>
  <read_first>src/app/(dashboard)/office/page.tsx</read_first>
  <action>
Migrate office/page.tsx (277 lines) React chrome from var(--*) and hardcoded hex to UUI Tailwind tokens.

ALLOWLIST (DO NOT MODIFY these imports or their internals):
- PhaserGame (dynamic import)
- AgentPanel
- EventBridge

Only migrate React chrome: layout wrapper, controls, toast system, connection indicator.

1. Dynamic import loading fallback (line 14):
   `style={{ ... color: 'var(--text-primary)' }}` -> use `className="text-primary"` (keep layout styles like display/align/justify as Tailwind: `flex items-center justify-center h-full`)

2. Connection indicator colors (line 20-22):
   ```
   const CONNECTION_COLORS = {
     connected: 'bg-success',     // was #22c55e
     connecting: 'bg-warning',    // was #eab308
     disconnected: 'bg-error',    // was #ef4444
   }
   ```
   Update consumers to use className instead of inline style for the indicator dot.

3. Toast message color (line 51, `color: '#e5e7eb'`):
   Replace with `text-muted` or `text-secondary` className (light gray text on dark toast).

4. Toast system colors (lines 81-83):
   ```
   const TOAST_COLORS = {
     success: 'text-success',   // was #22c55e
     error: 'text-error',       // was #ef4444
     info: 'text-info',         // was #3b82f6
   }
   ```
   Update consumers to use className.

5. Toast background (lines 107-108):
   `background: '#1f2937', color: '#e5e7eb'` -> `className="bg-surface-elevated text-secondary"` (dark surface with muted text -- toast overlay).

6. Any remaining var(--*) inline styles: migrate using standard token mapping.

IMPORTANT: Do NOT touch anything inside PhaserGame, AgentPanel, or EventBridge components. Only the React wrapper JSX in this file.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-d && grep -c "var(--" src/app/\(dashboard\)/office/page.tsx | grep -q "^0$" && echo "PASS: zero var(--*)" || echo "FAIL: var(--*) still present"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "var(--" src/app/(dashboard)/office/page.tsx` returns 0
- `grep "#22c55e\|#eab308\|#ef4444\|#3b82f6\|#e5e7eb\|#1f2937" src/app/(dashboard)/office/page.tsx` returns 0 matches
- `grep "PhaserGame\|AgentPanel\|EventBridge" src/app/(dashboard)/office/page.tsx` still returns matches (imports preserved)
- `grep "text-success\|text-error\|text-info\|bg-success\|bg-warning\|bg-error\|bg-surface-elevated" src/app/(dashboard)/office/page.tsx` returns matches
  </acceptance_criteria>
  <done>Office page React chrome has zero var(--*) inline styles and zero hardcoded hex colors. Phaser canvas internals (PhaserGame, AgentPanel, EventBridge) are untouched.</done>
</task>

</tasks>

<verification>
- `grep -c "var(--" src/app/(dashboard)/office/page.tsx` returns 0
- No hardcoded hex colors remain in React chrome
- PhaserGame, AgentPanel, EventBridge imports are unchanged
</verification>

<success_criteria>
Office page React chrome renders with zero var(--*) inline styles and zero hardcoded hex colors. Phaser canvas component imports and internals are preserved exactly as-is.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wave-c-agents-+-office/03-04-SUMMARY.md`
</output>
