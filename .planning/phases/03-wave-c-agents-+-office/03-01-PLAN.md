---
phase: 03-wave-c-agents-+-office
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/agents/page.tsx
  - src/components/AgentOrganigrama.tsx
autonomous: true
requirements: [WAVC-03]

must_haves:
  truths:
    - "Agents listing page renders with zero var(--*) inline styles"
    - "AgentOrganigrama SVG renders with zero var(--*) inline styles"
    - "Hardcoded hex STATUS_COLORS replaced with UUI semantic tokens"
    - "Agent status colors still visually distinguish idle/working/error/offline/thinking/queued/executing_tool"
  artifacts:
    - path: "src/app/(dashboard)/agents/page.tsx"
      provides: "Agents listing page with UUI tokens"
      contains: "text-primary"
    - path: "src/components/AgentOrganigrama.tsx"
      provides: "Agent org chart with UUI tokens"
      contains: "text-primary"
  key_links:
    - from: "src/app/(dashboard)/agents/page.tsx"
      to: "src/components/AgentOrganigrama.tsx"
      via: "import AgentOrganigrama"
      pattern: "import.*AgentOrganigrama"
---

<objective>
Migrate agents listing page and AgentOrganigrama component from legacy var(--*) inline styles and hardcoded hex colors to UUI Tailwind tokens.

Purpose: Eliminate all legacy design system references from the agents listing surface.
Output: Two files with zero var(--*) and zero hardcoded hex status colors.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate agents/page.tsx to UUI tokens</name>
  <files>src/app/(dashboard)/agents/page.tsx</files>
  <read_first>src/app/(dashboard)/agents/page.tsx</read_first>
  <action>
Migrate all 28 var(--*) inline styles and hardcoded hex colors to UUI Tailwind classes in agents/page.tsx.

Token mapping (proven in Phase 1+2):
- `var(--text-primary)` -> `text-primary` className
- `var(--text-secondary)` -> `text-secondary` className
- `var(--text-muted)` -> `text-muted` className
- `var(--surface)` -> `bg-surface` className
- `var(--surface-elevated)` -> `bg-surface-elevated` className
- `var(--card)` -> `bg-card` className
- `var(--border)` -> `border-border` className (with `border` for border-width)
- `var(--accent)` -> `bg-accent`/`text-accent` className
- `var(--background)` -> `bg-background` className
- `var(--font-heading)` -> `font-heading` className
- `var(--font-body)` -> `font-body` className

Replace hardcoded hex STATUS_COLORS map with Tailwind class map:
```
const STATUS_COLORS: Record<AgentStatus, string> = {
  idle: 'text-success',        // was #4ade80
  working: 'text-warning',     // was #f59e0b
  error: 'text-error',         // was #ef4444
  offline: 'text-muted',       // was #6b7280
  thinking: 'text-info',       // was #3b82f6
  queued: 'text-warning',      // was #eab308
  executing_tool: 'text-warning', // was #f59e0b
}
```
Where STATUS_COLORS values are consumed as `style={{ color: statusColor }}`, replace with `className={statusColor}` instead.

For the department color (`department?.color ?? '#6366f1'`) and hardcoded badge background hex values (#f59e0b20, #3b82f620, #22c55e20): these are dynamic API-provided colors. Keep as inline style (same decision as Phase 2: dynamic API colors kept as inline style).

For `var(--warning, #FFD60A)` fallback pattern at line 254: replace with `text-warning` className.

Convert all `style={{ borderBottom: '1px solid var(--border)' }}` to `className="border-b border-border"`.
Convert all `style={{ borderTop: '1px solid var(--border)' }}` to `className="border-t border-border"`.
Convert all `style={{ backgroundColor: 'var(--surface)' }}` to `className="bg-surface"`.

Where elements have BOTH existing className AND style props, merge the Tailwind classes into className and remove the style prop entirely (or remove only the migrated properties from style if non-migratable properties remain).
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-d && grep -c "var(--" src/app/\(dashboard\)/agents/page.tsx | grep -q "^0$" && echo "PASS: zero var(--*) remaining" || echo "FAIL: var(--*) still present"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "var(--" src/app/(dashboard)/agents/page.tsx` returns 0
- `grep "text-primary\|text-muted\|bg-surface\|border-border\|text-warning\|text-success\|text-error\|text-info" src/app/(dashboard)/agents/page.tsx` returns multiple matches
- STATUS_COLORS map values are Tailwind class strings, not hex codes
  </acceptance_criteria>
  <done>agents/page.tsx has zero var(--*) inline styles and zero hardcoded hex status colors (except dynamic API-provided department colors kept as inline style per established pattern)</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AgentOrganigrama.tsx to UUI tokens</name>
  <files>src/components/AgentOrganigrama.tsx</files>
  <read_first>src/components/AgentOrganigrama.tsx</read_first>
  <action>
Migrate all 11 var(--*) inline styles in AgentOrganigrama.tsx SVG elements to UUI Tailwind classes.

This is an SVG-based component. For SVG elements:
- `style={{ color: 'var(--text-primary)' }}` on SVG text/elements -> `className="text-primary"` (SVG elements support className)
- `style={{ stroke: 'var(--border)' }}` on SVG lines/paths -> `className="stroke-border"` or use `stroke="currentColor"` with `className="text-border"` (per Phase 2 SVG stroke pattern)
- `style={{ fill: 'var(--surface)' }}` on SVG rects -> `className="fill-surface"`
- `style={{ color: 'var(--text-muted)' }}` -> `className="text-muted"`
- `style={{ color: 'var(--text-secondary)' }}` -> `className="text-secondary"`

For any remaining non-token inline styles (layout positioning like x, y, width, height on SVG elements), keep as inline style -- those are not token-related.

Use the `stroke=currentColor + className` pattern established in Phase 2 for SVG strokes.
  </action>
  <verify>
    <automated>cd /Users/mac/Documents/Work/uui-worktrees/terminal-d && grep -c "var(--" src/components/AgentOrganigrama.tsx | grep -q "^0$" && echo "PASS: zero var(--*) remaining" || echo "FAIL: var(--*) still present"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "var(--" src/components/AgentOrganigrama.tsx` returns 0
- `grep "text-primary\|text-muted\|text-secondary\|stroke-border\|fill-surface" src/components/AgentOrganigrama.tsx` returns matches for SVG token usage
  </acceptance_criteria>
  <done>AgentOrganigrama.tsx has zero var(--*) inline styles, SVG elements use Tailwind token classes</done>
</task>

</tasks>

<verification>
- `grep -c "var(--" src/app/(dashboard)/agents/page.tsx` returns 0
- `grep -c "var(--" src/components/AgentOrganigrama.tsx` returns 0
- No hardcoded hex colors in STATUS_COLORS map (dynamic API colors excluded)
</verification>

<success_criteria>
Both agents listing page and AgentOrganigrama render with zero var(--*) inline styles. Hardcoded status hex colors replaced with UUI semantic token classes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wave-c-agents-+-office/03-01-SUMMARY.md`
</output>
